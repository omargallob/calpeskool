<% unless params[:name] == "Photo-Gallery" %>
	<%= @page.body %>
<% else %>	
	<% @galleries.each do |gallery|%>
		<h2 class="gallery"><%= gallery.title %></h2>
		<ul class="gallery">
			<%= render :partial => "shared/front/images", :collection => gallery.uploads %>
		</ul>
	<% end %>
<% end %>
