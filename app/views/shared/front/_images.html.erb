<li><%= link_to(image_tag(images.photo.url(:thumb)),  images.photo.url(:original),:rel => "lightbox[#{images.gallery.title}]" ) if controller.controller_name == "home" %>
	<%= image_tag images.photo.url(:pagesize) if controller.controller_name == "galleries" %></li>
